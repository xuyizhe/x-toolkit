{
  "name": "root",
  "private": true,
  "author": "Yizhe Xu <barrenbass@gmail.com>",
  "workspaces": ["convert-wasm", "crypto-wasm", "wasm-example"],
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "dev:example": "lerna run --stream --scope @x-toolkit/wasm-example dev",
    "dev": "npm run dev:example",
    "test": "lerna run test",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
